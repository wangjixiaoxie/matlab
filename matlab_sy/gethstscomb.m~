%hs struct returned has mnvl and marked.
function [hs]=gethstcomb(hs,muvlsvec,acmuvec,ntvec,avls);

for ii=1:length(muvec)
    muind=muvec(ii);
    cntvl=aa; 
    if(ac(aa))
        hs(cntvl).hst=avls.hstac_comb{ntvl(aa)}{muind}
    hs(cntvl+1).hst=avls.hstmu_comb{ntvl}(aa)}{muind}
end
    
